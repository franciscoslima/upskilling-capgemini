package list02.adapter.bankCardReader;

public class OldCardReader {
    void readMagneticStripe(String stripeData) {
        System.out.println("Reading with magnetic stripe: " + stripeData);
    }
}
